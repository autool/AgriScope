services:
  postgis:
    image: postgis/postgis:14-3.2
    environment:
      POSTGRES_DB: farmland
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    volumes:
      - ./backend/scripts/init_db.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - ./backend/data/administrative_boundaries:/docker-entrypoint-initdb.d/data/administrative_boundaries:ro
      - ./backend/data/farmland:/docker-entrypoint-initdb.d/data/farmland:ro
      - postgis_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d farmland"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://admin:admin123@postgis:5432/farmland
      APP_ENV: production
      LOG_LEVEL: INFO
      MAX_IMAGERY_UPLOAD_BYTES: 10737418240
      MAX_IMAGERY_BATCH_UPLOAD_BYTES: ${MAX_IMAGERY_BATCH_UPLOAD_BYTES:-21474836480}
      MAX_UAV_UPLOAD_BYTES: 2147483648
      MAX_CONSULTATION_EVIDENCE_BYTES: 52428800
      MAX_FIELD_PHOTO_BYTES: ${MAX_FIELD_PHOTO_BYTES:-20971520}
      MAX_FIELD_VOICE_BYTES: ${MAX_FIELD_VOICE_BYTES:-104857600}
      MAX_FIELD_FORM_BYTES: ${MAX_FIELD_FORM_BYTES:-52428800}
      CHANGE_PREVIEW_MAX_DIMENSION: 1400
      IMAGERY_QUICKLOOK_MAX_DIMENSION: 1400
      MAX_IMAGERY_MOSAIC_PIXELS: ${MAX_IMAGERY_MOSAIC_PIXELS:-10000000}
      MAX_IMAGERY_REGISTRATION_PIXELS: ${MAX_IMAGERY_REGISTRATION_PIXELS:-10000000}
      MAX_IMAGERY_FUSION_PIXELS: ${MAX_IMAGERY_FUSION_PIXELS:-10000000}
      MAX_GROWTH_MONITORING_PIXELS: ${MAX_GROWTH_MONITORING_PIXELS:-10000000}
      IMAGERY_REGISTRATION_PREVIEW_MAX_DIMENSION: ${IMAGERY_REGISTRATION_PREVIEW_MAX_DIMENSION:-2048}
      SERVICE_HEALTH_PRIVATE_HOST_ALLOWLIST: ${SERVICE_HEALTH_PRIVATE_HOST_ALLOWLIST:-}
    depends_on:
      postgis:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - backend_storage:/app/storage
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  postgis_data:
  backend_storage:
